import React, {Component} from "react";

interface PokemonDetailSectionProps {
    title: Component,
    body: Component
}

export const PokemonDetailSection : React.FC<PokemonDetailSectionProps> = (props : PokemonDetailSectionProps) => {

    return <div/>
}